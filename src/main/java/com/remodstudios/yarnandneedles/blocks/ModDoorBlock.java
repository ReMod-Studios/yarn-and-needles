package com.remodstudios.yarnandneedles.blocks;

import net.minecraft.block.DoorBlock;

// me: mommy i want access wideners
// mom: we already have access wideners at home
// access wideners at home:
public class ModDoorBlock extends DoorBlock {
    public ModDoorBlock(Settings settings) {
        super(settings);
    }
}
